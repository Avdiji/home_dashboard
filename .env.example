# Copy to .env and fill in. Each device needs its own key.
# Generate one at https://login.tailscale.com/admin/settings/keys — tick
# "Reusable" so re-provisioning this device later doesn't burn through keys.
TS_AUTHKEY=
